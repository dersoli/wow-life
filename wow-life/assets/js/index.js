new Glider(document.querySelector('.aulas'), {
    slidesToScroll: 1,
    slidesToShow: 4.5,
    draggable: true,
    arrows: {
      prev: '.glider-prev',
      next: '.glider-next'
    }
});